# web-shorts

## What is this  
`web-shorts` is a simple Go project. The repository does not currently provide detailed documentation, but based on the name, it appears to be an experimental project related to web short applications or services.  

**Main files:**  
- `main.go` — the main file of the project  
- `utils/` — helper functions  
- `go.mod` — Go dependencies  

> Note: This repository lacks documentation and usage examples, so functionality may be incomplete or experimental.

---

## How to install and run

1. Clone the repository:
```bash
git clone https://github.com/LoLoL200/web-shorts.git
cd web-shorts 


# build the project
go build

# or run directly
go run main.go
